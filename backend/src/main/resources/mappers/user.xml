<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wonsi.statrip.model.repository.UserRepository">

	<select id="findUserByUserId" parameterType="String" resultType="com.wonsi.statrip.model.dto.UserDto">
	    select user_id AS userId, password, name, email, gender, age_group AS ageGroup, addr_group AS addrGroup
	    from user
	    where user_id = #{userId}
	</select>
	
	<select id="findUserByRefreshToken" parameterType="String" resultType="String">
		select user_id AS userId
		from token
		where refresh_token = #{refreshToken}
	</select>
	
	<select id="login" parameterType="UserDto" resultType="UserDto">
		select user_id AS userId, password, name, email, gender, age_group AS ageGroup, addr_group AS addrGroup
	    from user
		where user_id = #{userId} AND password = #{password}
	</select>
	
	<update id="setToken" parameterType="map">
	    UPDATE user
	    SET refresh_token = #{refreshToken}
	    WHERE user_id = #{userId}
  	</update>
	
</mapper>