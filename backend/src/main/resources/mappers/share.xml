<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wonsi.statrip.model.repository.ShareArticleRepository">
    <resultMap type="ShareArticleDto" id="article">
        <result column="article_no" property="articleNo"/>
        <result column="user_id" property="userId"/>
        <result column="plan_id" property="planId"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="created_at" property="createdAt"/>
        <result column="hit" property="hit"/>
    </resultMap>

    <insert id="writeArticle" parameterType="ShareArticleDto">
        insert into share_article (user_id, plan_id, title, content, hit, created_at)
        values (#{userId}, #{planId}, #{title}, #{content}, 0, now())
    </insert>

    <select id="listArticle" resultType="ShareArticleDto">
        select article_no AS articleNo, user_id AS userId, plan_id as planId, title, content, hit, created_at as createdAt
        from share_article
    </select>

    <select id="getArticle" resultType="ShareArticleDto">
        select article_no AS articleNo, user_id AS userId, plan_id as planId, title, content, hit, created_at as createdAt
        from share_article
        where article_no = #{articleNo}
    </select>


</mapper>